# OIDC Authentication Configuration
# The following environment variables are required to run the application with OIDC authentication

# OIDC Provider Configuration (Keycloak Example)
# Replace with your Keycloak instance details
OIDC_ISSUER=https://your-keycloak-domain.com/realms/your-realm-name
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret

# Callback URL - must match the redirect URI configured in your OIDC provider
OIDC_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Session Configuration
# Cookie password must be at least 32 characters long
OIDC_COOKIE_PASSWORD=your-very-long-and-secure-cookie-password-here-at-least-32-chars

# Optional Configuration
# OIDC_COOKIE_NAME=oidc-session
# OIDC_COOKIE_MAX_AGE=34560000
# OIDC_SCOPE=openid profile email
# OIDC_COOKIE_DOMAIN=

# For other OIDC providers, update the OIDC_ISSUER accordingly:
# Auth0: https://your-domain.auth0.com
# Google: https://accounts.google.com
# Microsoft: https://login.microsoftonline.com/your-tenant-id/v2.0
# Okta: https://your-domain.okta.com/oauth2/default